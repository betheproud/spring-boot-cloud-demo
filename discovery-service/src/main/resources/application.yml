---
spring:
  application:
    name: discovery-service

server:
  port: ${PORT:8761}

management:
  security:
    enabled: false



eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
